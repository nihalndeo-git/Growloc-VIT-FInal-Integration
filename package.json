{
  "name": "growloc",
  "private": true,
  "description": "Run npm scripts from this folder; they delegate to frontend/. Do not run npm from ai-service/ (Python only).",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  }
}
